# useForm Hook

Ejemplo de uso:
```
    const initialForm={
        name:'',
        age:'',
        email:''
    };

    const { formValues, handleInputChenge, reset } = useForm( initialForm );

```

